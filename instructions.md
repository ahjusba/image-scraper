In this exercise, your task is to create a simple web image scraper. The tech stack is React & Node.js. Your application should present to the user a web interface where they can enter a URL. The application should then visit that URL and download all available images. Those images should then be presented to the user for viewing and download. The application should not rely on any external accounts, and it must run locally, other than contacting the URL given for scraping it. It should be designed to use AWS as a persistence backend, but the AWS part should be a mock-up.

As your answer, you should return a link to a repository we can access
The repository should contain code and instructions on how to run the application, plus anything else you feel should be included
When creating code, anything goes. Using LLMs, outside help, everything is allowed. But the final result is graded as your own work
If you do use an LLM for coding, include prompts you used in a text file in the repository
Although in real life it is great to ask for clarification, in this exercise avoid that if possible. Regarding any details the instructions leave open, use your best judgment
After turning in your answer, we can arrange a time for the technical interview. This interview takes 1,5 hours and happens live at our office. You should come prepared with your own laptop, ready to present your solution.

Make sure your dev environment is set up so that you can demonstrate coding both with AI and without it
When coding without AI, you can still use any non-AI helpers such as syntax highlighting